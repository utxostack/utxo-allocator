import { List } from 'immutable';
import { Cell, Input } from '@ckb-lumos/lumos';
import { UnpackResult } from '@ckb-lumos/codec';
import { ResolvedInputs } from '../codec/buildingPacket';
export declare function inputCellsToResolvedInputs(inputs: List<Cell>, filter?: (value: Cell, index: number, iter: List<Cell>) => boolean): UnpackResult<typeof ResolvedInputs>;
export declare function resolvedInputsToInputCells(inputs: Input[], resolvedInputs: UnpackResult<typeof ResolvedInputs>): Cell[];
