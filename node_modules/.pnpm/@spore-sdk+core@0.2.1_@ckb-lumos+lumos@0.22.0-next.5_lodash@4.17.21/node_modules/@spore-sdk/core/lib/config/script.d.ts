import { ScriptId } from '../types';
import { SporeConfig, SporeScript, SporeScriptCategory } from './types';
/**
 * Get a specific SporeScriptCategory from SporeConfig by "scriptName".
 * Throws an error if the script doesn't exist.
 */
export declare function getSporeScriptCategory(config: SporeConfig, scriptName: string): SporeScriptCategory;
/**
 * Get a specific SporeScript from SporeConfig by "scriptName" with optional "scriptId" or "tags".
 * Throws an error if the script doesn't exist.
 */
export declare function getSporeScript(config: SporeConfig, scriptName: string): SporeScript;
export declare function getSporeScript(config: SporeConfig, scriptName: string, tags: string[]): SporeScript;
export declare function getSporeScript(config: SporeConfig, scriptName: string, scriptId: ScriptId): SporeScript;
/**
 * Get a specific SporeScript from SporeConfig by "scriptName".
 * Throws an error if the script doesn't exist.
 */
export declare function getLatestSporeScript(config: SporeConfig, scriptName: string): SporeScript;
/**
 * Get a specific SporeScript from SporeConfig by "scriptName" and "scriptId".
 * Throws an error if the script doesn't exist.
 */
export declare function getSporeScriptByScriptId(config: SporeConfig, scriptName: string, scriptId: ScriptId): import("./cache").SporeCategorizedScript;
/**
 * Get a specific SporeScript from SporeConfig by "scriptName" and "scriptId".
 * Throws an error if the script doesn't exist.
 */
export declare function getSporeScriptByTags(config: SporeConfig, scriptName: string, tags: string[]): SporeScript;
/**
 * Returns a boolean indicating weather if the target ScriptName exists in the SporeConfig.
 */
export declare function isSporeScriptCategorySupported(config: SporeConfig, scriptName: string): boolean;
/**
 * Returns a boolean indicating weather if the target ScriptId exists in the SporeConfig.
 * If "scriptName" is passed, it also checks whether the name of the target script matches.
 */
export declare function isSporeScriptSupported(config: SporeConfig, scriptId: ScriptId, scriptName?: string): boolean;
