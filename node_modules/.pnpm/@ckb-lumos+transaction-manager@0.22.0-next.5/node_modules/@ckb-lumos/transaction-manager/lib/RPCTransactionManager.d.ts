import { TransactionManager } from "./TransactionManager";
import { Store } from "./store";
/**
 * The easy way to create a `TransactionManager` by just providing an RPC URL.
 */
export declare class RPCTransactionManager extends TransactionManager {
    constructor({ rpcUrl, storage }: {
        rpcUrl: string;
        storage?: Store;
    });
}
//# sourceMappingURL=RPCTransactionManager.d.ts.map