import { Address } from '@ckb-lumos/base';
import { Config } from '@ckb-lumos/config-manager';
import { FromInfo } from '@ckb-lumos/common-scripts';
/**
 * Check if the target address is valid.
 */
export declare function isAddressValid(address: Address, config?: Config): boolean;
/**
 * Convert a FromInfo to a CKB address.
 */
export declare function fromInfoToAddress(fromInfo: FromInfo, config?: Config): Address;
