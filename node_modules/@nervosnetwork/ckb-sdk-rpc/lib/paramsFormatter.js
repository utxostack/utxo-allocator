"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const ckb_sdk_utils_1 = require("@nervosnetwork/ckb-sdk-utils");
const index_js_1 = require("./exceptions/index.js");
const formatter = {
    toOptional: (format) => (arg) => {
        if (!format || arg === undefined || arg === null) {
            return arg;
        }
        return format(arg);
    },
    toArray: (format) => (arg) => {
        if (typeof format !== 'function' || !Array.isArray(arg)) {
            return arg;
        }
        return arg.map(format);
    },
    toHash: (hash) => {
        if (typeof hash !== 'string') {
            throw new index_js_1.StringHashTypeException(hash);
        }
        return hash.startsWith('0x') ? hash : `0x${hash}`;
    },
    toNumber: (number) => {
        if (typeof number === 'bigint') {
            return `0x${number.toString(16)}`;
        }
        if (typeof number !== 'string') {
            throw new index_js_1.BigintOrHexStringTypeException(number);
        }
        if (!number.startsWith('0x')) {
            throw new index_js_1.HexStringWithout0xException(number);
        }
        return number;
    },
    toScript: (script) => {
        const { codeHash, hashType: hash_type } = script, rest = (0, tslib_1.__rest)(script, ["codeHash", "hashType"]);
        return Object.assign({ code_hash: formatter.toHash(codeHash), hash_type }, rest);
    },
    toOutPoint: (outPoint) => {
        if (!outPoint)
            return outPoint;
        const { txHash, index } = outPoint, rest = (0, tslib_1.__rest)(outPoint, ["txHash", "index"]);
        return Object.assign({ tx_hash: formatter.toHash(txHash), index: formatter.toNumber(index) }, rest);
    },
    toInput: (input) => {
        if (!input)
            return input;
        const { previousOutput, since } = input, rest = (0, tslib_1.__rest)(input, ["previousOutput", "since"]);
        return Object.assign({ previous_output: formatter.toOutPoint(previousOutput), since: formatter.toNumber(since) }, rest);
    },
    toOutput: (output) => {
        if (!output)
            return output;
        const { capacity, lock, type = null } = output, rest = (0, tslib_1.__rest)(output, ["capacity", "lock", "type"]);
        return Object.assign({ capacity: formatter.toNumber(capacity), lock: formatter.toScript(lock), type: type ? formatter.toScript(type) : type }, rest);
    },
    toDepType: (type) => {
        if (type === 'depGroup') {
            return 'dep_group';
        }
        return type;
    },
    toCellDep: (cellDep) => {
        if (!cellDep)
            return cellDep;
        const { outPoint = null, depType = 'code' } = cellDep, rest = (0, tslib_1.__rest)(cellDep, ["outPoint", "depType"]);
        return Object.assign({ out_point: formatter.toOutPoint(outPoint), dep_type: formatter.toDepType(depType) }, rest);
    },
    toRawTransaction: (transaction) => {
        if (!transaction)
            return transaction;
        const { version, cellDeps = [], inputs = [], outputs = [], outputsData: outputs_data = [], headerDeps: header_deps = [] } = transaction, rest = (0, tslib_1.__rest)(transaction, ["version", "cellDeps", "inputs", "outputs", "outputsData", "headerDeps"]);
        const formattedInputs = inputs.map(input => formatter.toInput(input));
        const formattedOutputs = outputs.map(output => formatter.toOutput(output));
        const formattedCellDeps = cellDeps.map(cellDep => formatter.toCellDep(cellDep));
        const tx = Object.assign({ version: formatter.toNumber(version), cell_deps: formattedCellDeps, inputs: formattedInputs, outputs: formattedOutputs, outputs_data,
            header_deps }, rest);
        return tx;
    },
    toPageNumber: (pageNo = '0x1') => formatter.toNumber(pageNo),
    toPageSize: (pageSize = '0x32') => {
        const size = ckb_sdk_utils_1.JSBI.BigInt(`${pageSize}`);
        const MAX_SIZE = 50;
        const MIN_SIZE = 0;
        if (ckb_sdk_utils_1.JSBI.greaterThan(size, ckb_sdk_utils_1.JSBI.BigInt(MAX_SIZE)))
            throw new index_js_1.PageSizeTooLargeException(pageSize, MAX_SIZE);
        if (ckb_sdk_utils_1.JSBI.lessThan(size, ckb_sdk_utils_1.JSBI.BigInt(MIN_SIZE)))
            throw new index_js_1.PageSizeTooSmallException(pageSize, MIN_SIZE);
        return formatter.toNumber(`0x${size.toString(16)}`);
    },
    toReverseOrder: (reverse = false) => !!reverse,
    toOutputsValidator: (outputsValidator) => {
        if (!outputsValidator)
            return undefined;
        const VALIDATORS = ['default', 'passthrough'];
        if (VALIDATORS.indexOf(outputsValidator) > -1) {
            return outputsValidator;
        }
        throw new index_js_1.OutputsValidatorTypeException();
    },
    toBoolean: (value) => {
        return !!value;
    },
    toTransactionProof: (proof) => {
        if (!proof)
            return proof;
        const { blockHash: block_hash, witnessesRoot: witnesses_root } = proof, rest = (0, tslib_1.__rest)(proof, ["blockHash", "witnessesRoot"]);
        return Object.assign({ block_hash,
            witnesses_root }, rest);
    },
};
exports.default = formatter;
//# sourceMappingURL=paramsFormatter.js.map